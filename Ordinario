#include <stdio.h>
#include <stdlib.h>

struct Alumno{
    char matricula[10];
    char carrera[20];
    int semestre;
    char nombre[23];
    float calif[5][5];
};

struct Persona{
    char nombre[30];
    int edad;
    char genero;
    char fn[8];

    struct Alumno *ptrAlumno;
    struct Persona *sigPersona;
};

int main(){

    int opcion;

    struct Persona *inicio = NULL;
    struct Persona *aux = NULL;

    do{
        printf("\n   MENU PRINCIPAL");
        printf("\n1. Dar de alta");
        printf("\n2. Mostrar");
        printf("\n3. Buscar");
        printf("\n4. Eliminar");
        printf("\n5. Salir");
        printf("\n\nOpcion: ");
        scanf("%d",&opcion);

        switch(opcion){

            case 1:
                printf("\nAlta de alumno\n");
                break;

            case 2:
                printf("\nMostrar alumnos\n");
                break;

            case 3:
                printf("\nBuscar alumno\n");
                break;

            case 4:
                printf("\nEliminar alumno\n");
                break;

            case 5:
                printf("\nFin del programa\n");
                break;

            default:
                printf("\nOpcion invalida\n");
        }

    }while(opcion != 5);

    return 0;
}
